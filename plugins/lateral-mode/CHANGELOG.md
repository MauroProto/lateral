# Changelog

## 0.2.0 - 2026-05-13

### Added

- Added Claude Code plugin manifest and marketplace metadata.
- Added `lateral-debug`, `lateral-design`, and `lateral-control` skills.
- Added `reframer` and `verifier` subagents.
- Added plugin-scoped Claude hooks using `${CLAUDE_PLUGIN_ROOT}`.
- Added plugin-scoped control binary `lateral-mode`.
- Added vendored Python engine support for self-contained plugin execution.
